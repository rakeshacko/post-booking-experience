import { BehindTheScenesDesktop } from "@/components/behind-the-scenes/BehindTheScenesDesktop";

/**
 * Desktop "behind the scenes" view — the mobile prototype on the left, the work
 * happening behind every wait (and the agents that erase it) on the right.
 * Best viewed on a wide screen.
 */
export default function BehindTheScenesPage() {
  return <BehindTheScenesDesktop />;
}
